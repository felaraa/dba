# credenciais reais — NUNCA versionar
config/db.yaml
config/rawdb.yaml
config/datadb.yaml

# artefatos Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
build/
dist/

# saídas locais de análise
*.local.md
out/

# artefatos do modo batch (SQL/planos extraídos ao vivo — nunca versionar)
temp/
